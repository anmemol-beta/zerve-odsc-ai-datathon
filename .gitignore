/datas/
/venv/
/.venv/
/figures/
__pycache__/
.DS_Store
web/out/
web/.next/
web/node_modules/
# dist/ and models/v3/ are checked in on purpose — they're the offline
# fallback bundle that the deployment falls back to when zerve.variable()
# can't reach the canvas. Generated by `python export_artifacts.py`.

# secrets — NEVER commit
.env
.env.local
.env.*.local
*.key
api_keys.txt
llm_cache/
